services:
  spotify-download-tg-bot:
    image: ghcr.io/zetoofficial/spotify-download-tg-bot:latest
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./cache:/app/cache
      - ./data:/app/data
